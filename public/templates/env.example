# Copy this to .env.local and fill in the values. Never commit real secrets.

# App
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Database and auth (example: Supabase)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Add the keys your stack needs above.
